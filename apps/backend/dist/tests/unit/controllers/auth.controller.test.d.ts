/**
 * AuthController — Unit Tests
 *
 * authController methods are wrapped in asyncHandler(fn),
 * which requires (req, res, next). Errors are forwarded to next().
 */
export {};
//# sourceMappingURL=auth.controller.test.d.ts.map